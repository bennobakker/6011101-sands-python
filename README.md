# 6011101-sands-python
This project generates *sine* and *step* signals and operates on them.

*signals.py* contains functions to generate and modify signals

*script.py* runs the functions, plots original modified signals, and saves figure

*tests* includes small tests to check that all functions work correctly

*pyproject.toml* lists the project data, providing settings and information
